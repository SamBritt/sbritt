import React, { useState } from 'react'
import { motion } from 'framer-motion'

export const NavTitle = ({activeNavText}) => (
    
    <h1
    id="navTextBold"
    
    key = "ttt"
    >
    {activeNavText}
    </h1>
)
